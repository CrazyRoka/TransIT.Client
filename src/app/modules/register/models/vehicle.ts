import { VehicleType } from './vehicle-type';

export class Vehicle {
  id?: number;
  vehicleType?: VehicleType;
  vincode?: string;
  inventoryId?: string;
  regNum?: string;
  brand?: string;
  model?: string;
}
