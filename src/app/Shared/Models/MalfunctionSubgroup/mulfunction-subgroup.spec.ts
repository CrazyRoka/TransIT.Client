import { MulfunctionSubgroup } from './mulfunction-subgroup';

describe('MulfunctionSubgroup', () => {
  it('should create an instance', () => {
    expect(new MulfunctionSubgroup()).toBeTruthy();
  });
});
